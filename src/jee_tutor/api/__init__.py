"""Stable API contracts exposed by the tutor runtime.

Modules in this package define request, response, and status contracts. They
may depend on domain models, but must not depend on concrete runtime adapters.
"""
