"""Student longitudinal profile package.

Import concrete modules directly, for example ``jee_tutor.profile.models``.
The package initializer intentionally avoids re-exporting service modules so
profile model imports do not initialize LLM or storage adapters.
"""
